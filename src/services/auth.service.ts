// Cliente frontend (axios) removido: este archivo no pertenece al backend.
// La lógica del cliente HTTP (api.ts y servicios de frontend) debe residir
// en la aplicación React/React Native dentro de la carpeta `src/` del frontend.

export {};
